---
schemaVersion: treeseed.execution-plan/v1
id: sdk-living-review-revision
revision: 5
proposalId: b2c45a28-3f9d-4b56-a8d4-cd7f49af205d
proposalRevision: 4
proposalDigest: 7bb82a7b25ea9412df29630a23623f4f48ef83f422e656856c4a633b21806db9
status: accepted
workItems:
  - id: implement-revision-fixture
    activity: acting
    agentClass: engineering
    objective: >-
      For revision 1, create tests/fixtures/living-graph-review-revision.ts exporting
      the literal INITIAL_CANDIDATE and create
      tests/unit/agent-capacity/capacity/living-graph-review-revision.test.ts asserting
      that literal. Do not include REVISION_CONFIRMED in revision 1. On a returned
      revision, consume the exact review finding, replace both fixture and assertion
      with REVISION_CONFIRMED, and preserve focused test coverage.
    estimate:
      minimumSeconds: 60
      expectedSeconds: 180
      maximumSeconds: 300
    produces:
      - id: implementation-candidate
        outputType: source-candidate
        required: true
        acceptanceCriteria:
          - The candidate is committed from the assignment worktree.
          - Focused SDK tests pass in the isolated execution guest.
    requires: []
    requiredCapabilities:
      - treeseed.engineering.code-change
    acceptanceCriteria:
      - A focused regression fixture and test exist in the SDK package.
      - Revision 1 contains INITIAL_CANDIDATE and does not contain REVISION_CONFIRMED.
      - The candidate passes npm test -- --run tests/unit/agent-capacity/capacity/living-graph-review-revision.test.ts.
      - Any reviewer findings from an earlier revision are fully resolved.
    assumptions:
      - The SDK staging source is available through the provider source cache.
    blockers: []
    reviewerAgentClasses:
      - reviewer
  - id: review-revision-fixture
    activity: reviewing
    agentClass: review
    objective: >-
      Review the exact implementation candidate commit supplied by the acting output.
      Run npm test -- --run
      tests/unit/agent-capacity/capacity/living-graph-review-revision.test.ts. Require
      revision 1 because it intentionally contains INITIAL_CANDIDATE and omits
      REVISION_CONFIRMED. The finding must name that exact commit and require both
      fixture and assertion to use REVISION_CONFIRMED. Approve only the distinct
      revised commit after the focused command passes.
    estimate:
      minimumSeconds: 45
      expectedSeconds: 120
      maximumSeconds: 240
    produces:
      - id: review-decision
        outputType: review-disposition
        required: true
        acceptanceCriteria:
          - The disposition identifies the exact reviewed candidate.
          - Rejection or revision includes actionable findings.
          - Approval is emitted only after focused verification passes.
    requires:
      - workItemId: implement-revision-fixture
        outputType: source-candidate
    requiredCapabilities:
      - treeseed.engineering.review
    acceptanceCriteria:
      - The exact source candidate is inspected and tested.
      - The review disposition is evidence-backed.
      - The source fixture contains a machine-checkable REVISION_CONFIRMED assertion.
      - A deficient candidate is returned to acting without changing DAG semantics.
    assumptions: []
    blockers: []
---

# Living graph review-revision acceptance

This operational execution plan is a bounded acceptance fixture for the real
estimate-derived living graph and Kata-isolated SDK assignment path.
