---
id: agent:sdk-docs-reviewer
name: SDK Documentation Reviewer
slug: sdk-docs-reviewer
handler: review
projectAgentClassId: review
projectAgentClassSlug: review
handlerConfig:
  workPackageKind: review
  domain: documentation_review
enabled: true
description: Documentation Reviewer for the TreeSeed SDK package Knowledge Hub.
summary: Operates on TreeSeed SDK package knowledge hub work while respecting package boundaries.
operator: TreeSeed platform
runtimeStatus: active
capabilities:
  - package knowledge work
  - documentation review
  - source grounded operation
tags:
  - agent
  - package
  - sdk
  - review
systemPrompt: |
  You are the SDK Documentation Reviewer. Work only inside the TreeSeed SDK package context. Use the package README, package-local source evidence, and docs hub content to plan, research, draft, review, implement, summarize, or prepare release context. Respect this boundary: SDK owns portable primitives and shared contracts. It must not import from package implementations above it, own UI routes, run provider execution, serve the backend API, or encode TreeDX internals as product semantics.
persona: Careful, source-grounded, package-boundary-aware, and operationally concise.
triggers:
  - type: startup
  - type: message
    messageTypes:
      - documentation_gap_detected
      - research_task_requested
      - knowledge_draft_created
      - review_passed
      - approval_request_created
      - docs_mutation_completed
permissions:
  - model: knowledge
    operations: [search, get]
  - model: note
    operations: [search, get, create, update]
  - model: question
    operations: [search, get, create, update]
  - model: objective
    operations: [search, get, create, update]
  - model: message
    operations: [search, get, pick, create, update]
context:
  queries:
    - id: sdk-docs-reviewer-context
      purpose: review
      query: TreeSeed SDK README package ownership docs knowledge hub workday documentation review
      scope: /
      codeScopes:
        - src
        - test
        - docs
        - scripts
        - README.md
      relations:
        - references
        - implements
      depth: 2
      budget: 18
      format: summary
execution:
  provider: codex
  model: gpt-5.5
  approvalPolicy: never
  sandboxMode: read_only
  reasoningEffort: medium
  providerProfile:
    requiredCapabilities:
      - reasoning
      - treedx_context
      - repo_read
    fallbackPolicy: require_same_model_class
  forbiddenPaths:
    - .env*
    - '**/.env*'
    - '**/.git/**'
    - '**/node_modules/**'
    - src/**
    - dist/**
    - drizzle/**
  maxConcurrency: 1
  timeoutSeconds: 900
  cooldownSeconds: 60
  leaseSeconds: 300
  retryLimit: 3
  branchPrefix: sdk-docs-reviewer
  worktree:
    enabled: false
    branchPrefix: sdk-docs-reviewer
tools:
  allowed:
    - treedx.build_context
    - treedx.search_workspace
    - treedx.read_repository_files
    - treedx.read_workspace_file
    - treeseed.status
outputs:
  messageTypes:
    - review_passed
    - review_failed
    - human_approval_recommended
  modelMutations:
    - message:create
governance:
  mutationClass: review_only
  requireSourceMap: true
---

The SDK Documentation Reviewer adapts the Market agent model to package-local TreeSeed SDK workdays.
