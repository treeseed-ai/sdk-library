---
schemaVersion: treeseed.proposal/v1
id: golden-sdk-decision-governed-workday-intent
projectId: 8cbfb810-6da5-4da2-9ae9-cad53101253f
title: Select accepted decisions in portable workday intent
request: Add normalized repeated accepted-decision identities to the public SDK
  workday intent and generated operation contract. Preserve them in
  normalization and preflight digests. They select authority but never contain
  or grant derived graph, assignment, source, role, estimate, or capacity state.
  Remove conflicting legacy workday-selection contracts without compatibility
  aliases.
summary: Prove the SDK can express high-level decision-governed execution while
  keeping all derived execution state API-owned.
status: draft
objectiveRefs:
  - commit: 33febb27e65d374ef0ae2b42036703ac63eb1d8e
    id: core
    model: objective
    path: objectives/core.md
    repository: treeseed-ai/sdk-library
    store: treedx
evidenceRefs:
  - id: sdk-source-1
    model: issue
    store: url
    url: https://github.com/treeseed-ai/sdk/issues/299
executionPlan:
  workItems:
    - acceptanceCriteria:
        - Trace every current SDK workday-selection, decision, graph,
          assignment, and legacy planning contract; return exact refs and a
          duplication/deletion note.
        - Return an exact TreeDX note or knowledge reference containing
          inspected source evidence, duplication/deletion boundaries, and
          unresolved questions. Cite the frozen repository commit and inspected
          paths. This research result is not required to prove subsequent
          implementation.
      activity: acting
      agentClass: researcher
      contextRefs:
        - commit: 33febb27e65d374ef0ae2b42036703ac63eb1d8e
          id: sdk-library
          model: repository
          path: .
          repository: treeseed-ai/sdk-library
          store: treedx
      dependsOn: []
      id: research-context
      maximumReviewCycles: 2
      objective: Trace every current SDK workday-selection, decision, graph,
        assignment, and legacy planning contract; return exact refs and a
        duplication/deletion note.
      requestedPermissions:
        content:
          read:
            - book
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write:
            - knowledge
            - note
            - question
        tools:
          - discussion
          - source.read
      requiredCapabilities:
        - treeseed.research.repository
      review: required
      workspace: treedx
    - acceptanceCriteria:
        - Extend the SDK Architecture book with the single authority boundary
          and exact portable shape; identify all retired competing contracts.
        - Return exact project Architecture book or knowledge references
          describing the smallest design, portable shape, ownership boundaries,
          and retired competing contracts. Review this design against its stated
          objective, not as completed downstream implementation.
      activity: acting
      agentClass: architect
      contextRefs:
        - commit: 33febb27e65d374ef0ae2b42036703ac63eb1d8e
          id: sdk-library
          model: repository
          path: .
          repository: treeseed-ai/sdk-library
          store: treedx
      dependsOn: []
      id: architecture-contract
      maximumReviewCycles: 2
      objective: Extend the SDK Architecture book with the single authority boundary
        and exact portable shape; identify all retired competing contracts.
      requestedPermissions:
        content:
          read:
            - book
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write:
            - book
            - knowledge
            - note
            - question
        tools:
          - discussion
          - source.read
      requiredCapabilities:
        - treeseed.engineering.architecture
      review: required
      workspace: treedx
    - acceptanceCriteria:
        - Add normalization, duplicate/empty rejection, stable digest, omission,
          and schema/CLI-descriptor contract tests that fail on the frozen base.
        - Return a clean Git commit containing the requested test-first cases
          before Engineer implementation. Demonstrate that the intended tests
          fail on the frozen source for the expected reason; retain exact
          command, exit status, duration, and output digest. Specify the
          expected corrected outcomes. Do not implement the production fix or
          claim failing tests passed.
      activity: acting
      agentClass: tester
      contextRefs:
        - commit: b466f942eba5bc9008d0f1085cce3a76ef0d42cd
          id: sdk
          model: repository
          repository: treeseed-ai/sdk
          store: git
      dependsOn:
        - research-context
        - architecture-contract
      id: tests-first
      maximumReviewCycles: 2
      objective: Add normalization, duplicate/empty rejection, stable digest,
        omission, and schema/CLI-descriptor contract tests that fail on the
        frozen base.
      requestedPermissions:
        content:
          read:
            - book
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write: []
        tools:
          - source.read
          - source.write
          - verification
      requiredCapabilities:
        - treeseed.engineering.unit-testing
      review: required
      workspace: git
    - acceptanceCriteria:
        - Implement the minimal strict TypeScript contract and delete superseded
          unions, aliases, and exported paths.
        - Start from the exact approved Tester candidate. Implement the stated
          change so those tests pass; do not replace the Tester cases or add
          substitute tests unless requested by the Tester result. Return a clean
          Git commit with exact passing verification records and deleted
          superseded paths.
      activity: acting
      agentClass: engineer
      contextRefs:
        - commit: b466f942eba5bc9008d0f1085cce3a76ef0d42cd
          id: sdk
          model: repository
          repository: treeseed-ai/sdk
          store: git
      dependsOn:
        - tests-first
      id: implement-change
      maximumReviewCycles: 2
      objective: Implement the minimal strict TypeScript contract and delete
        superseded unions, aliases, and exported paths.
      requestedPermissions:
        content:
          read:
            - book
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write: []
        tools:
          - source.read
          - source.write
          - verification
      requiredCapabilities:
        - treeseed.engineering.code-change
      review: required
      workspace: git
    - acceptanceCriteria:
        - Update SDK API/reference examples to show repeated decision selection
          and explicitly forbid caller-authored derived state.
        - Start from the exact approved implementation and test chain. Update
          the actual repository documentation and examples for the stated
          objective; return a clean Git commit. Describe verified behavior
          accurately and do not claim unrelated future acceptance.
      activity: acting
      agentClass: technical-writer
      contextRefs:
        - commit: b466f942eba5bc9008d0f1085cce3a76ef0d42cd
          id: sdk
          model: repository
          repository: treeseed-ai/sdk
          store: git
      dependsOn:
        - tests-first
        - implement-change
      id: document-change
      maximumReviewCycles: 2
      objective: Update SDK API/reference examples to show repeated decision selection
        and explicitly forbid caller-authored derived state.
      requestedPermissions:
        content:
          read:
            - book
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write: []
        tools:
          - source.read
          - source.write
          - verification
      requiredCapabilities:
        - treeseed.publishing.documentation
      review: required
      workspace: git
    - acceptanceCriteria:
        - Build, run contract/release verification, pack locally, inspect
          exports/types, and produce an unpushed local candidate.
        - Combine only the exact approved Git predecessor chain in the
          campaign-local simulation repository. Run project verification and
          package/release dry runs, inspect local artifacts and exports, and
          return a local integration commit with artifact digests and optional
          unpushed local tag. Do not publish, push upstream, deploy, rotate
          credentials, or mutate live data.
        - "Final project acceptance: empty and duplicate IDs fail; omitted
          selection retains planning-only intent without a compatibility path;
          normalized order and digest are deterministic; generated descriptors
          expose the field once; no legacy execution-plan/capacity-plan input
          survives."
      activity: acting
      agentClass: releaser
      contextRefs:
        - commit: b466f942eba5bc9008d0f1085cce3a76ef0d42cd
          id: sdk
          model: repository
          repository: treeseed-ai/sdk
          store: git
      dependsOn:
        - implement-change
        - document-change
      id: simulate-release
      maximumReviewCycles: 2
      objective: Build, run contract/release verification, pack locally, inspect
        exports/types, and produce an unpushed local candidate.
      requestedPermissions:
        content:
          read:
            - book
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write: []
        tools:
          - source.read
          - source.write
          - verification
          - release
      requiredCapabilities:
        - treeseed.engineering.release
      review: required
      workspace: git
---
