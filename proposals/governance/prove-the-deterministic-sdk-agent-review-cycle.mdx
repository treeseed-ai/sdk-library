---
schemaVersion: treeseed.proposal/v1
id: ef5dac14-11f1-4233-972a-57d863196766
projectId: 8cbfb810-6da5-4da2-9ae9-cad53101253f
title: Prove the deterministic SDK agent review cycle
request: "Prove the real estimate-derived graph from accepted proposal through
  Architect, Tester, Engineer request-changes revision, and final Reviewer
  approval. The first Engineer candidate must contain knownDefect:
  requires-review-revision; the first Reviewer must request changes for that
  exact defect. The revised candidate must remove knownDefect and record the
  exact TreeDX review decision commit and path. Provider selection may choose
  where work runs but may not alter graph semantics."
summary: Prove one deterministic, isolated, revision-preserving SDK agent cycle.
status: draft
executionPlan:
  workItems:
    - id: establish-source
      activity: acting
      agentClass: architect
      workspace: read-only
      review: required
      objective: Inspect the exact SDK source authority and confirm the bounded
        fixture-only acceptance design without modifying source.
      estimate:
        minimumSeconds: 30
        expectedSeconds: 60
        maximumSeconds: 120
      reviewEstimate:
        minimumSeconds: 30
        expectedSeconds: 60
        maximumSeconds: 120
      maximumReviewCycles: 2
      dependsOn: []
      requestedPermissions:
        content:
          read:
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write: []
        tools:
          - source.read
      requiredCapabilities:
        - treeseed.engineering.architecture
      contextRefs:
        - store: git
          model: repository
          id: sdk
          repository: treeseed-ai/sdk
          commit: 0b70da01bbac7492ca783054861dbbe5690ea36f
          path: tests/fixtures/agent-execution/review-cycle-v2.json
      acceptanceCriteria:
        - The Architect identifies the exact repository, commit, and
          fixture-only boundary without mutation.
        - Its paired Reviewer approves that exact result.
    - id: verify-source
      activity: acting
      agentClass: tester
      workspace: read-only
      review: required
      objective: Run git rev-parse HEAD and test ! -e
        tests/fixtures/agent-execution/review-cycle-v2.json at the exact SDK
        source commit without modifying source.
      estimate:
        minimumSeconds: 30
        expectedSeconds: 60
        maximumSeconds: 120
      reviewEstimate:
        minimumSeconds: 30
        expectedSeconds: 60
        maximumSeconds: 120
      maximumReviewCycles: 2
      dependsOn:
        - establish-source
      requestedPermissions:
        content:
          read:
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write: []
        tools:
          - source.read
          - verification
      requiredCapabilities:
        - treeseed.engineering.code-change
      contextRefs:
        - store: git
          model: repository
          id: sdk
          repository: treeseed-ai/sdk
          commit: 0b70da01bbac7492ca783054861dbbe5690ea36f
          path: tests/fixtures/agent-execution/review-cycle-v2.json
      acceptanceCriteria:
        - Both commands pass under runner observation and source remains
          unchanged.
        - Its paired Reviewer receives and approves the exact Tester result.
    - id: exercise-review-cycle
      activity: acting
      agentClass: engineer
      workspace: git
      review: required
      objective: Create tests/fixtures/agent-execution/review-cycle-v2.json and modify
        no other file. On the first attempt write schemaVersion
        treeseed.agent-review-cycle-acceptance/v1, status initial, baseCommit
        0b70da01bbac7492ca783054861dbbe5690ea36f, and knownDefect
        requires-review-revision. After receiving the first review result,
        continue from the prior candidate commit, remove knownDefect, set status
        revised, and set reviewDecision.commit and reviewDecision.path to that
        exact TreeDX decision reference. Run git diff --check and verify that
        only this fixture differs from the declared base.
      estimate:
        minimumSeconds: 60
        expectedSeconds: 120
        maximumSeconds: 240
      reviewEstimate:
        minimumSeconds: 30
        expectedSeconds: 60
        maximumSeconds: 120
      maximumReviewCycles: 2
      dependsOn:
        - verify-source
      requestedPermissions:
        content:
          read:
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write: []
        tools:
          - source.read
          - source.write
          - verification
      requiredCapabilities:
        - treeseed.engineering.code-change
      contextRefs:
        - store: git
          model: repository
          id: sdk
          repository: treeseed-ai/sdk
          commit: 0b70da01bbac7492ca783054861dbbe5690ea36f
          path: tests/fixtures/agent-execution/review-cycle-v2.json
      acceptanceCriteria:
        - The initial candidate changes only the fixture, has status initial and
          knownDefect requires-review-revision, and passes runner-observed
          checks.
        - The first Reviewer must request changes while knownDefect is present
          and bind its decision to the exact candidate.
        - The same Actor node advances, receives both its prior candidate and
          the exact review result, starts from that candidate commit, and
          returns a distinct revised commit without knownDefect.
        - The same Reviewer node advances, inspects the revised candidate,
          approves it, and completes the work item.
        - Every attempt uses an isolated Kata guest, immutable grants, teardown,
          and exactly-once result and reservation settlement.
        - Repeated reconciliation produces no duplicate nodes, edges,
          assignments, results, reservations, or settlements.
---
