---
schemaVersion: treeseed.proposal/v1
id: 4be5c0f2-4ca0-4a24-8cc9-6d1c64df5ea3
projectId: 8cbfb810-6da5-4da2-9ae9-cad53101253f
title: Verify capability-bound living SDK execution graph
request: "Exercise the real accepted proposal work item through capability-bound
  admission, the canonical assignment-result contract, isolated Actor execution,
  exact predecessor delivery, independent Reviewer execution, and terminal
  reconciliation. The single declared acting work item and review: required are
  the only plan authority: reconciliation derives the Actor node, paired
  Reviewer node, and review-pair edge. Pre-decision review judges this plan
  before Actor execution; predecessor results are expected only from the
  post-Actor Reviewer. Provider selection chooses where the immutable nodes
  execute and cannot alter their topology or semantics."
summary: Prove the canonical proposal-to-Actor-to-Reviewer path without a fixed
  pipeline or duplicate plan authority.
status: ready
executionPlan:
  workItems:
    - id: verify-living-graph
      activity: acting
      agentClass: architect
      workspace: read-only
      review: required
      objective: Inspect the current SDK source and report how the living execution
        graph contracts are implemented, distinguishing verified behavior from
        remaining work.
      estimate:
        minimumSeconds: 120
        expectedSeconds: 240
        maximumSeconds: 480
      reviewEstimate:
        minimumSeconds: 60
        expectedSeconds: 120
        maximumSeconds: 240
      maximumReviewCycles: 2
      dependsOn: []
      requestedPermissions:
        content:
          read:
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write: []
        tools:
          - source.read
      requiredCapabilities:
        - treeseed.engineering.architecture
      acceptanceCriteria:
        - The report cites exact current SDK source evidence and distinguishes
          implemented behavior from remaining work.
        - The Actor returns the one canonical treeseed.assignment-result/v1
          result with exact references, runner-observed verification, usage, and
          diagnostics; no proposal-specific output taxonomy is introduced.
        - Admission evidence binds the Actor's treeseed.engineering.architecture
          demand to one satisfying provider offer without changing the graph
          topology.
        - Execution evidence proves the Actor ran in its isolated Kata guest
          with the exact read-only SDK source authority and that its workspace
          was cleaned up after completion.
        - "Reconciliation projects exactly one paired Reviewer from review:
          required, delivers the Actor's exact predecessor result, records an
          exact work-review disposition, and settles both attempts exactly
          once."
        - A repeated reconciliation creates no duplicate node, edge, assignment,
          result, reservation, or settlement.
        - Any passing verification command was directly observed to exit zero in
          the assignment workspace.
---
