---
schemaVersion: treeseed.proposal/v1
id: golden-sdk-decision-governed-workday-intent-v4
projectId: 8cbfb810-6da5-4da2-9ae9-cad53101253f
title: Select accepted decisions in portable workday intent
request: "Add decisionIds?: string[] to WorkdayIntent and expose it exactly once
  as repeatable/comma-separated --decision input for workdays plan. Validate
  bounded nonblank identities and reject duplicates after trimming. Normalize
  the IDs into deterministic lexical order in the portable request handed to the
  API; the API remains the authority that computes intent and preflight digests
  from that normalized request. Retain agentSelection, allocation, profile,
  project, timing, and communication inputs because they are high-level workday
  policy—not derived capacity state. Retire operatorConstraints and reject
  caller-authored graph, executionPlan, capacityPlan, demand, assignment,
  source, role, estimate, reservation, usage, and provider-selection fields
  without aliases."
summary: Prove the SDK can express high-level decision-governed execution while
  keeping all derived execution state API-owned.
status: draft
objectiveRefs:
  - commit: 65106c908bca127ea45d11b3f6b54905878d191c
    id: sdk-core
    model: objective
    path: objectives/core.md
    repository: repo_a3c347e2abcd09af
    store: treedx
evidenceRefs:
  - id: sdk-source-1
    model: issue
    store: url
    url: https://github.com/treeseed-ai/sdk/issues/299
executionPlan:
  workItems:
    - acceptanceCriteria:
        - Return exact source refs for SDK workday selection, decision, graph,
          assignment, and retired planning contracts, with a
          duplication/deletion note; do not claim implementation or test
          verification.
      activity: acting
      agentClass: researcher
      contextRefs:
        - commit: 65106c908bca127ea45d11b3f6b54905878d191c
          digest: sha256:65c10939426530c2060f70519f52b9f158e8085b4fa4086c7f27324a53df85b5
          id: sdk-core
          model: book
          path: books/sdk-core.md
          repository: repo_a3c347e2abcd09af
          revision: 1
          store: treedx
        - commit: 65106c908bca127ea45d11b3f6b54905878d191c
          id: sdk-library
          model: repository
          path: .
          repository: repo_a3c347e2abcd09af
          store: treedx
      dependsOn: []
      estimate:
        expectedSeconds: 300
        maximumSeconds: 480
        minimumSeconds: 180
        rationale: The assigned research has no dependencies and requires targeted
          inspection of four contract/test seams, reconciliation of existing
          predecessor findings, explicit separation of WorkdayIntent from
          governance executionPlan fields, and a precise duplication/deletion
          handoff. The expected estimate allows time to trace descriptor reuse
          and API-owned derived-state boundaries without running builds or
          tests; the upper bound covers ambiguity in generated/export
          references.
      id: research-context
      maximumReviewCycles: 2
      objective: Trace every current SDK workday-selection, decision, graph,
        assignment, and legacy planning contract; return exact refs and a
        duplication/deletion note.
      requestedPermissions:
        content:
          read:
            - book
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write:
            - knowledge
            - note
            - question
        tools:
          - discussion
          - source.read
      requiredCapabilities:
        - treeseed.research.repository
      review: required
      reviewEstimate:
        expectedSeconds: 300
        maximumSeconds: 480
        minimumSeconds: 180
        rationale: Cross-cutting review of lifecycle, descriptors, selection bindings,
          generated/export seams, and tests, including exact-reference tracing
          and governance-schema separation; upper bound covers ambiguity in
          generated contracts.
      workspace: treedx
    - acceptanceCriteria:
        - Commit an SDK Architecture Knowledge page in the exact Architecture
          Book defining the single authority boundary, portable WorkdayIntent
          shape, and retired competing contracts, with exact source evidence.
      activity: acting
      agentClass: architect
      contextRefs:
        - commit: 65106c908bca127ea45d11b3f6b54905878d191c
          digest: sha256:c740b7fbb79681781b95e311e838ef3e231873f16b1cb4dd8c8fbe5112a4eb02
          id: sdk-architecture
          model: book
          path: books/architecture.md
          repository: repo_a3c347e2abcd09af
          revision: 1
          store: treedx
        - commit: 65106c908bca127ea45d11b3f6b54905878d191c
          id: sdk-library
          model: repository
          path: .
          repository: repo_a3c347e2abcd09af
          store: treedx
      dependsOn: []
      estimate:
        expectedSeconds: 120
        maximumSeconds: 150
        minimumSeconds: 90
        rationale: Focused architecture-contract work is limited to specifying and
          recording one authority boundary across the existing lifecycle and
          shared command-descriptor seams, identifying retired competing
          contracts, and preserving the distinction between WorkdayIntent and
          proposal governance executionPlan. The existing decisionIds field and
          single --decision binding reduce discovery effort; time is reserved
          for checking all acceptance-criterion implications and documenting
          dependencies and risks.
      id: architecture-contract
      maximumReviewCycles: 2
      objective: Extend the SDK Architecture book with the single authority boundary
        and exact portable shape; identify all retired competing contracts.
      requestedPermissions:
        content:
          read:
            - book
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write:
            - book
            - knowledge
            - note
            - question
        tools:
          - discussion
          - source.read
      requiredCapabilities:
        - treeseed.engineering.architecture
      review: required
      reviewEstimate:
        expectedSeconds: 120
        maximumSeconds: 150
        minimumSeconds: 90
        rationale: Focused review of the SDK authority boundary, retired competing
          contracts, retained policy inputs, and distinction from proposal
          governance; existing decision and CLI seams reduce discovery.
      workspace: treedx
    - acceptanceCriteria:
        - Commit tests for normalization, duplicate and empty rejection, stable
          digest, omission, and schema/CLI-descriptor behavior that fail on the
          frozen SDK base; report the exact failing checks.
      activity: acting
      agentClass: tester
      contextRefs:
        - commit: 1186bff3b400fe442642013b2de93e7f0d4939df
          id: sdk
          model: repository
          repository: treeseed-ai/sdk
          store: git
      dependsOn:
        - research-context
        - architecture-contract
      estimate:
        expectedSeconds: 180
        maximumSeconds: 240
        minimumSeconds: 120
        rationale: Focused contract-test work spans the existing lifecycle and
          command-descriptor test seams. The base already has decisionIds, one
          shared --decision binding, and basic omission/empty-selection tests,
          reducing discovery time; the estimate reserves time to add trim-based
          duplicate rejection, deterministic lexical-order and
          digest-equivalence cases, planning-only omission assertions,
          descriptor single-exposure checks, and strict rejection/exclusion
          coverage for caller-authored derived fields while distinguishing
          proposal executionPlan from WorkdayIntent.
      id: tests-first
      maximumReviewCycles: 2
      objective: Add normalization, duplicate/empty rejection, stable digest,
        omission, and schema/CLI-descriptor contract tests that fail on the
        frozen base.
      requestedPermissions:
        content:
          read:
            - book
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write: []
        tools:
          - source.read
          - source.write
          - verification
      requiredCapabilities:
        - treeseed.engineering.unit-testing
      review: required
      reviewEstimate:
        expectedSeconds: 180
        maximumSeconds: 240
        minimumSeconds: 120
        rationale: Review of tests for trimming, post-trim duplicates, lexical
          normalization, digest equivalence, omission, descriptor uniqueness,
          and derived-state rejection; existing basic tests reduce setup.
      workspace: git
    - acceptanceCriteria:
        - Implement the minimal strict TypeScript contract, make the approved
          Tester checks pass, and delete superseded unions, aliases, exports,
          and caller-authored derived-state inputs.
      activity: acting
      agentClass: engineer
      contextRefs:
        - commit: 1186bff3b400fe442642013b2de93e7f0d4939df
          id: sdk
          model: repository
          repository: treeseed-ai/sdk
          store: git
      dependsOn:
        - tests-first
      estimate:
        expectedSeconds: 300
        maximumSeconds: 420
        minimumSeconds: 180
        rationale: Focused TypeScript implementation across the identified lifecycle
          contract and shared descriptor/export seams. The field and CLI binding
          already exist, reducing discovery; time is needed to implement
          trim-based validation, post-trim duplicate rejection, deterministic
          lexical normalization before digesting, remove operatorConstraints and
          superseded derived-state paths, preserve planning-only omission and
          retained policy inputs, and inspect the resulting type/schema/export
          surface. The range includes dependency alignment with tests-first and
          review of all acceptance criteria.
      id: implement-change
      maximumReviewCycles: 2
      objective: Implement the minimal strict TypeScript contract and delete
        superseded unions, aliases, and exported paths.
      requestedPermissions:
        content:
          read:
            - book
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write: []
        tools:
          - source.read
          - source.write
          - verification
      requiredCapabilities:
        - treeseed.engineering.code-change
      review: required
      reviewEstimate:
        expectedSeconds: 300
        maximumSeconds: 420
        minimumSeconds: 180
        rationale: Review of strict lifecycle normalization, duplicate rejection,
          deterministic digest preparation, legacy-contract removal, and
          resulting type/schema/export surfaces; existing seams reduce
          discovery.
      workspace: git
    - acceptanceCriteria:
        - Commit SDK API and reference examples for repeated decision selection
          and the prohibition on caller-authored derived state; verify the
          documentation examples.
      activity: acting
      agentClass: technical-writer
      contextRefs:
        - commit: 1186bff3b400fe442642013b2de93e7f0d4939df
          id: sdk
          model: repository
          repository: treeseed-ai/sdk
          store: git
      dependsOn:
        - tests-first
        - implement-change
      estimate:
        expectedSeconds: 135
        maximumSeconds: 180
        minimumSeconds: 90
        rationale: The repository already has the decisionIds field and one shared
          --decision descriptor, but documentation must be aligned with the
          post-implementation normalization and strict retirement contract
          across a small README/API-reference surface. The estimate includes
          updating repeated/comma-separated decision examples, omission and
          planning-only guidance, API ownership and forbidden-derived-state
          language, checking generated reference consistency, and accounting for
          dependency-driven wording changes after tests-first and
          implementation. The upper bound covers stale generated descriptions or
          examples and the risk of distinguishing proposal governance
          executionPlan from WorkdayIntent without documenting an unintended
          compatibility path.
      id: document-change
      maximumReviewCycles: 2
      objective: Update SDK API/reference examples to show repeated decision selection
        and explicitly forbid caller-authored derived state.
      requestedPermissions:
        content:
          read:
            - book
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write: []
        tools:
          - source.read
          - source.write
          - verification
      requiredCapabilities:
        - treeseed.publishing.documentation
      review: required
      reviewEstimate:
        expectedSeconds: 135
        maximumSeconds: 180
        minimumSeconds: 90
        rationale: Review of API/reference updates for repeated or comma-separated
          decisions, planning-only omission, governed authority, API-owned
          derived state, and no compatibility alias; upper bound covers stale
          generated examples.
      workspace: git
    - acceptanceCriteria:
        - Integrate the exact approved Git predecessors locally, run build and
          contract/release checks, pack and inspect exports and types, and
          return an unpushed candidate satisfying the proposal-wide final gates.
      activity: acting
      agentClass: releaser
      contextRefs:
        - commit: 1186bff3b400fe442642013b2de93e7f0d4939df
          id: sdk
          model: repository
          repository: treeseed-ai/sdk
          store: git
      dependsOn:
        - implement-change
        - document-change
      estimate:
        expectedSeconds: 180
        maximumSeconds: 270
        minimumSeconds: 120
        rationale: The repository already provides standard build, contract-test,
          release-verification, packing, and export surfaces, and the relevant
          SDK seams are known. The minimum covers a focused build plus
          contract/release verification and export inspection. The expected
          estimate allows local packing, generated declaration review, and clean
          unpushed-candidate confirmation after both implementation and
          documentation dependencies. The maximum reserves time for diagnosing
          stale generated artifacts, export/type drift, descriptor duplication,
          or release-check failures while preserving the acceptance-boundary
          review.
      id: simulate-release
      maximumReviewCycles: 2
      objective: Build, run contract/release verification, pack locally, inspect
        exports/types, and produce an unpushed local candidate.
      requestedPermissions:
        content:
          read:
            - book
            - knowledge
            - objective
            - proposal
            - decision
            - note
            - question
          write: []
        tools:
          - source.read
          - source.write
          - verification
          - release
      requiredCapabilities:
        - treeseed.engineering.release
      review: required
      reviewEstimate:
        expectedSeconds: 180
        maximumSeconds: 270
        minimumSeconds: 120
        rationale: Review of build, contract/release checks, local packing, generated
          declarations and exports, descriptor uniqueness, and clean
          unpushed-candidate confirmation; upper bound covers stale artifacts
          and release drift.
      workspace: git
---
